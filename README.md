# TurtleBank

## Fictional online banking service

### Usage

Dev - `yarn dev`
Build - `yarn build`
Start - `yarn start`
Lint - `yarn lint`

### Issues & PRs

Use `gitmoji` or https://emojipedia.org to use the commit format
